#include "main.h"
#include <stdio.h>
/**
 * is_uper -  checks for weather upper letter or not
 * @c: third int
 * Return: 1 if upper else 0
 */
 int _isupper(int c)
 {
	if (c >= 65 & c <=90)
		return (1);
	else
	return (0);
}

